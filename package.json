{
  "name": "pi-gitnexus-plus",
  "version": "0.7.0",
  "description": "GitNexus knowledge graph integration for pi — enriches searches with call chains, execution flows, and blast radius. Supports stdio and HTTP transports.",
  "author": "Brad Huffman",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ppsplus-bradh/pi-gitnexus-plus.git"
  },
  "homepage": "https://github.com/ppsplus-bradh/pi-gitnexus-plus#readme",
  "bugs": {
    "url": "https://github.com/ppsplus-bradh/pi-gitnexus-plus/issues"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-extension",
    "gitnexus",
    "knowledge-graph",
    "code-intelligence",
    "call-graph",
    "agent"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run lint && npm run typecheck && npm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test",
    "lint": "biome check src/",
    "lint:fix": "biome check --fix src/"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.74",
    "@earendil-works/pi-coding-agent": ">=0.74",
    "@earendil-works/pi-tui": ">=0.74",
    "typebox": ">=1.0",
    "zod": ">=3.25"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cross-spawn": "7.0.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/cross-spawn": "6.0.6",
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ],
    "skills": [
      "./skills"
    ],
    "image": "https://github.com/ppsplus-bradh/pi-gitnexus-plus/raw/master/media/screenshot.png"
  }
}
